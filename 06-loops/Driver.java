public String frontTimes(String str, int n) {
  if (str.length()<=3){
    String front = str;
    while (n > 0) {
    return front;
    n=n-1;
    }} else {
    String front = str.substring(0,3);
    while (n > 0) {
    return front;
    n=n-1;
    }
    }
    
    }
